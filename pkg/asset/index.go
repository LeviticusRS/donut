package asset

const (
    // Manifests specify what archives are contained within an index and the build of each archive.
    Manifests = 255

    // The current number of indexes.
    // TODO(hadyn): Replace this with a more dynamic way.
    IndexCount = 21
)
